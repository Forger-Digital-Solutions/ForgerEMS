name: ventoy-core

on:
  push:
  pull_request:

jobs:
  verify-and-build:
    runs-on: windows-latest
    defaults:
      run:
        shell: pwsh

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Offline verification (blocking)
        run: .\ventoy-core\Verify-VentoyCore.ps1

      - name: Build release (blocking)
        run: .\Tools\build-release.ps1

      - name: Online verification (non-blocking)
        continue-on-error: true
        run: .\ventoy-core\Verify-VentoyCore.ps1 -Online

      - name: Upload generated release artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ventoy-core-release
          path: |
            release/ventoy-core/**
            .verify/**
